This is how the backend will work:

- There will be a socket server with connections to each client
- Authentication is done in a single http exchange with Flask-Login before the client opens the socket connection
- The current hand, as well as inter-hand data will be stored in redis. The current hand will be stored as a .act-parsable string in redis
- Redis message queue will mediate between socketio and a celery app
- The celery worker will perform tasks that do the game logic, update the game state in redis, and emit events back to the clients

Other stuff:
    - Any db calls should be written in mplib, not here
    - The game container will be initialized with its table_id. Assumed to be an env var right now
        - It can deduce everything else it needs from making db calls (through mplib)

To-Do's (Old):
    0. Read up on pokerface .act method so we can just store the string to parse to rebuild the pokergame on task start
    1. Write socketio server routes, figure out how to namespace properly, etc.
    2. Write logic for playing one hand
    3. Add redis so that we can persist data between hands
    4. Write remaining logic so that we can play a full game
    5. Dockerize the backend
    6. Write tests :(
    7. Write frontend code so we can see it, demo, etc.

To-Do's (Old):
    1. Add auth- it will be easier to test the game if we have real fake users in a db [✔]
        - Write the SQL [✔]
        - Connect SQLAlchemy to PostgreSQL [✔]
        - Update mplib accordingly [✔]
        - Add Flask-Login [✔]
        - Make some tests [✔]
            - Make tests for auth [✔]
            - Make tests for mpgame controller [✔]
    2. Move frontend over to full redux, use slices, etc. [✔]
        - Move any large states over to datastore [✔]
        - Test with redux dev tool [✔]
    3. Write out Login -> Game View frontend code, and hook up to backend auth [x]
        - Render login form [✔]
        - Wire login form to Flask-Login [✔]
            - Try test user [✔]
        - Make sure cookies work [x]
            - Update DB as appropriate for JWT auth [x]
            - Rewrite UserMixin [x]
            - Add JWT auth [x]
            - Decouple mpgame controller from mplib auth [x]
            - Write tests for http and socket [x]
        - Render register form [x]
        - Wire to backend [✔]
        - Write transition to game [✔]
            - Likely just a connect button with a hardcoded ip [✔]
    4. Start adding frontend event handlers [x]
        - Get initial http login to work [✔]
            - Use same controller for now [✔]
        - Implement connect [✔]
        - Write hand/game state shape [✔]
        - Implement add_player and deal [x]
        - Continue from there [x]
    5. Add backend event handlers [x]
        - Implement connect [x]
        - Write create_public_data [x]
        - Write add_player and deal [x]
        - Continue until game logic is sound [x]
    6. Demo Time! [x]

Distant Future:
    - Add auth
    - Add compliance
    - Add stripe or other
    - Add support / ticketing system
    - Add professional ux/ui
    - Demo and look for funding

THINGS TO REMEMBER:
    - We are using /login route for both classic login, as well as session verification and set-up for game socket
        - This will need to be separated later when they aren't the same thing, or maybe cookies will take care of all this
    - Check out Geocomply (it's a product) for IP compliance


To-Do's (April 20, 2023):
    1. Fix auth [✔]
        - UPDATE: We will implement refresh tokens later, for now, we have very long lived JWT access tokens for testing
    2. Reassess Cloud Architecture [x]
        - This is an open ended question, but ultimately we don't want to change this again.
        - For one thing, we need to decide how decoupled we want to be. With a strong auth 
          scheme, we can confidently implement an extremely loosely coupled architecture. We
          should strive for an async, event-driven architecture, with a central message-
          brokering cluster
        - We need to remember that performance must be highest at our only state-based
          component: the game socket.
        - Current stance:
            - Very micro microservices
            - Cloud pub/sub
    3. Make game service [x]
        - Create serialization interface, likely a decorator that takes game_id and provides obj [x]
        - Add Logic [x]
            - ...

To-Do's (May 1, 2023):
    1. Create easy redis interface [✔]
        - We need this for serialization and deserialization of hands, and eventually refresh token session stuff
    2. Create simple shell script for deployment [✔] UPDATE: It's good enough lol. Don't need any extra stuff for now
        - Start docker [x]
        - Start game flask app [✔]
            - Create test users [x]
        - Start redis [x]
        - Start celery [✔]
        - Start mpfrontend [✔]
        - Add options for easy temporary configuration [x]
        - (Maybe) dockerize everything [x]
    3. Make fully serializable poker hand object [x]
        - Add features to stringify, etc for all poker actions [✔]
            - deal [✔]
            - bets [✔]
            - showdowns [x]
            etc.
        - Add validation for hands [x]
            - fail when not possible, throw descriptive errors [x]
            - May need keys with meta data about past actions [x]
            - write tests to ensure soundness of critical game logic [x]
    4. Make simple storage of hands in redis [x]
        - Make one key for playing one game [✔]
        - Iterate on this simple design until we can serialize and deserialize a full game
        - Plan: Check socket connection metadata for player id
    5. Actually use RedisJSON [x]
        - Don't remember why it doesn't work now, but eventually we will want it
          Simple write as string, loads, dumps method is fine for now
     

To-Do's (May 4, 2023):
    1. Dockerize the mpgame and celery processes, preferrably separately [x]
        - Add setup.py so we can pip install mpgame
        - Add git permissions so we can clone mpgame
        - Debug til it works
    2. Verify that the docker instance integrates with mpfrontend
        - Make sure the whole login/join process works as before
        - Get to the point that connect event fires seemlessly with auth
    3. Write core game logic
        - The legs are there for creating a very simple demo.
          We know how the celery/broker/socket thing works,
          so we just need to write all the appropriate routes
        - Follow through on this until we have a simple demo
        - There may be hiccups, we reevaluate then
    4. Iterate over the TODO's:
        - There are many spots where we said we would fix it later
        - There are many [x]'s still in this document
          that are best practices we skipped over and need to be 
          done
        - These need to be fixed. Let's make our architecture sound
        - Write tests while going over this
        - Reach 90% test coverage
    5. Make it a little prettier, within reason
        - We will still need a UX designer
